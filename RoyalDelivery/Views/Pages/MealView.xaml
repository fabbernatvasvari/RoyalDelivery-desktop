<UserControl x:Class="RoyalDelivery.Views.Pages.MealView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewmodels="clr-namespace:RoyalDelivery.ViewModels.Pages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pages="clr-namespace:RoyalDelivery.ViewModels.Pages" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MealViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >

    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Ételek listája" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListView ItemsSource="{Binding Meals}" Height="300" SelectedItem="{Binding SelectedMeal, Mode=TwoWay}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" />
                        <GridViewColumn Header="Név" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Leírás" DisplayMemberBinding="{Binding Description}" />
                        <GridViewColumn Header="Ár" DisplayMemberBinding="{Binding Price}" />
                        <GridViewColumn Header="Étterem ID" DisplayMemberBinding="{Binding Restaurantid}" />
                    </GridView>
                </ListView.View>
                <ListView.Style>
                    <Style TargetType="ListView">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Meals.Count}" Value="0">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListView">
                                            <Grid>
                                                <TextBlock Text="Nincs megjeleníthető adat🥀."
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Opacity="0.6"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>
            </ListView>
            <Button Content="Törlés"
Command="{Binding DeleteSelectedCommand}"
Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
