<UserControl x:Class="RoyalDelivery.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <StackPanel Margin="10">
        <TextBlock Text="Felhasználók listája" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <ListView ItemsSource="{Binding Users}" Height="300" SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Felhasználónév" DisplayMemberBinding="{Binding Username}" />
                    <GridViewColumn Header="Keresztnév" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Header="Vezetéknév" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" />
                    <GridViewColumn Header="Cím" DisplayMemberBinding="{Binding Address}" />
                </GridView>
            </ListView.View>
        </ListView>

        <Button Content="Törlés"
    Command="{Binding DeleteSelectedCommand}"
    Margin="0,10,0,0" />
    </StackPanel>
</UserControl>
