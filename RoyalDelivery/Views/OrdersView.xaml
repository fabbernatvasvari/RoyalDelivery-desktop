<UserControl x:Class="RoyalDelivery.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoyalDelivery.Views"
              xmlns:viewmodel="clr-namespace:RoyalDelivery.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="Silver">
    <UserControl.DataContext>
        <viewmodel:OrderViewModel />
    </UserControl.DataContext>
    <StackPanel Margin="10">
        <Label>Osztályok</Label>
        <ListView ItemsSource="{Binding Classes}"
              SelectedItem="{Binding SelectedClass, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Osztály" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Létszám" DisplayMemberBinding="{Binding OrderCount}" />
                </GridView>
            </ListView.View>
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Classes.Count}" Value="0">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListView">
                                        <Grid>
                                            <TextBlock Text="Nincs megjeleníthető osztály."
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Opacity="0.6"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
            <!-- ItemTemplate maradhat -->
        </ListView>

        <Button Content="Törlés"
            Command="{Binding DeleteSelectedCommand}"
            Margin="0,10,0,0" />
    </StackPanel>
</UserControl>